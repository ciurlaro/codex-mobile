# Third-party notices

Codex Mobile consumes `codex-agent-client:0.1.0` and
`codex-agent-runtime-android:0.1.0` (GPL-3.0-or-later). The runtime artifact
packages Codex App Server `0.145.0` (Apache-2.0). Kotlin, kotlinx, AndroidX,
Okio, and all locked runtime dependencies retain their own licences. Exact
versions and hashes are recorded in `docs/technical/sbom.cdx.json`.

Chat math is rendered locally by RaTeX `0.1.13` and its bundled KaTeX fonts
(MIT). No WebView or remote math service is used.
